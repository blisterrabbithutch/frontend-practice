{
  "compilerOptions": {
    "outDir": "./dist/",
    // не позволяет использовать переменные без явного указания типа. явный any пропустит
    "noImplicitAny": true,
    // модульная система - commonJs, es6 итд - отвечает за систему импортов
    "module": "es6",
    // в эту версию компилируем output файл
    "target": "es5",
    // пишем синтаксис реакта который будет компилироваться
    "jsx": "react",
    // обрабатываем ts & js
    "allowJs": true,
    // ниже - node/classic. юзаем node
    "moduleResolution": "node",
    // ниже - позволяет работать с пакетами которые используют
    // common js как с обычными пакетами с помощью import
    "esModuleInterop": true,
    // ниже - вместо import * as React from 'react'
    "allowSyntheticDefaultImports": true,
    // для абсолютного импорта
    "baseUrl": "."
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  }
}
